version: "2"
services:
  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
      - "27018:27018"
    volumes_from:
      - mongodbdata

  mongodbdata:
    image: tianon/true
    volumes:
      - /data/db

  app:
    build: .
    container_name: express-test-app
    hostname: express-mongo
    mem_limit: "2G"
    memswap_limit: "2G"
    cpuset: "2"
    ports:
    - "13000:3000"
    restart: always
    privileged: true
    depends_on:
      - mongodb
    links:
      - mongodb
    dns:
      - 8.8.8.8
      - 8.8.4.4
    tty: true
    volumes:
      - "./../:/usr/src/app"